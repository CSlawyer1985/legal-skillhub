# 更新日志

## 0.2.0 — 2026-05-11

范围锁定：仅限美国法律（联邦 / 州 / 地方），对 50 个州 + 哥伦比亚特区 + 领土以及联邦/州/地方互动进行明确的多法域处理。

- 在 frontmatter 中添加 `jurisdiction: US (federal, state, local)`
- 重写 `description` 以标明仅限美国的范围，并将联邦优先（preemption）/ 多州比较命名为触发词
- 标题和引言更新为"美国法律"，并附对非美国来源的明确拒绝规则
- 在输入要求中添加 **非美国标记**：检测到欧盟/英国/外国来源时停止并拒绝
- 将"跨法域分析"部分替换为"多法域处理（美国境内）"，涵盖联邦 / 州 / 地方层级、联邦优先（始终升级）、多州比较模式，以及地方自治（home-rule）与迪隆规则（Dillon's Rule）的互动
- 添加"范围外的外国法律"失败模式
- 细化"法域不匹配"失败模式，点明美国内部模式（优先、地方自治）
- 升级触发更新：检测到非美国来源、联邦/州/地方优先、条例与州法冲突、州法与联邦法冲突
- 输出模板现将法域行拆分为层级 + 具体对象，并添加联邦优先状态和州际冲突字段（两者均标记交由律师处理，不作声明）
- "超出范围"部分明确将非美国法律列为第一个排除类别

## 0.1.0 — 2026-05-11

第二轮：与 skill-creator 对齐。

- 重写 `description` frontmatter 以点名明确的触发语境（依据 skill-creator 指引对抗触发不足）
- 将内联准则表精简为摘要；深度内容指向 `references/canons_of_construction.md`
- 在抽象输出模板旁添加一个完整的输入/输出工作示例
- 在推理足以支撑之处添加"为什么"理由，以柔化僵硬的 MUST 表述
- 将三节快速参考检查清单合并为单一预检清单
- 将"一致性与常识"部分折叠为一段
- 在 `references/index.md` 中为每个文件添加"何时阅读："指引
- 将更新日志噪声从 `SKILL.md` 移入本文件

## 0.1.0 — 2026-05-11（第一轮）

依据 2026-05-11 日期的 QA 报告进行生产加固。新增：

- 受众部分（初级律师 / 合规 / 律师助理；需律师审查）
- 工作形态声明（渐进式判断，Accretive Judgment）
- 超出范围部分（判例法、宪法、条约、法律选择、起草、游说、最终意见）
- 含五项最低要求和停止并询问规则的输入要求
- 委托门槛与问责部分（草稿页眉、具名律师要求）
- 置信度区间（高 / 中 / 低）
- 含七个强制停止并转交触发的升级逻辑
- 失败模式（建议漂移、特权泄露、问责缺口、引注过时、法域不匹配）
- 结构化输出模板
- frontmatter 中的 `version` 和 `author`

解决了 frontmatter 的法域不匹配（原为 `jurisdiction: US`，而示例使用 GDPR 风格表述），方法是使框架明确与法域无关，每次分析应用一个法域。
