# 强条原文库（已演进为检索式金标准）

> 本文件原用于「逐条拆分的强条原文」。现 skill 已 pivot 为**检索式金标准**：
> - 本地金标准 = 用户提供的官方 PDF（文本型直接抽，扫描型走 OCR）+ `references/golden/` 金标准库。
> - agent 用到时由 `scripts/extract_pdf.py` 按需抽取，保留原始编号，不预拆。
> - 联网新发现按「模式三 · 知识回流」沉淀至 `references/golden/`（默认 ⏳ 待核对）。
>
> 因此本文件不再承载逐条数据。如需逐条金标准，请在 `references/golden/` 中把相关条目核对后晋升为 ✅（可由 `golden.py lookup` 直接调用）。
> 条款版本真相见 `references/规范速查表.md`。
