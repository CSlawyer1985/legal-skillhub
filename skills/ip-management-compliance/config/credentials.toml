# 专利检索网站凭证配置
# ⚠️ 此文件包含敏感凭证，必须加入 .gitignore，切勿提交到版本控制
# 请将下方占位符替换为实际凭证
# 如无实际凭证，建议将此文件从技能包中移除或设置 enabled=false

[uyanip]
enabled = false              # true=启用自动使用，false=禁用
username = "YOUR_USERNAME"
password = "YOUR_PASSWORD"
remark = "欧燕专利检索平台"

# ⚠️ 安全提示：部署前请确认此文件不会被公开泄露
# 如仅使用 PatSeek / tavily-search，可完全删除此配置文件

