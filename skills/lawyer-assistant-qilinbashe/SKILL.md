---
name: 律师助手
description: 你的AI律师助手。73个专业版+7个路由/工具+26个工具+1个推图技能（共107个技能文件），覆盖民事/刑事/劳动/非诉/破产/执行/行政/仲裁全领域。
tags: [法律, 律师, 诉讼, 合同, 刑事辩护, 劳动争议, 破产, 社区法律, 法律咨询, beginner-friendly]
version: 4.4.1
author: 七邻八舍
platforms: [workbuddy, skillhub, hermes, claude-code, codex]
category: legal
trigger_words:
  # 核心
  - 法律
  - 律师
  - 起诉
  - 纠纷
  - 合同
  - 打官司
  # 民间借贷
  - 借钱
  - 欠钱
  - 借条
  - 欠条
  - 不还钱
  - 借贷
  - 欠债
  - 讨债
  - 催收
  # 交通事故
  - 车祸
  - 被撞
  - 交通事故
  - 肇事
  - 理赔
  - 保险赔偿
  # 劳动争议
  - 被辞退
  - 开除
  - 裁员
  - 扣工资
  - 欠薪
  - 劳动仲裁
  - 工伤
  - 辞职
  - 补偿金
  - 赔偿金
  # 婚姻家事
  - 离婚
  - 出轨
  - 家暴
  - 抚养权
  - 财产分割
  - 彩礼
  - 继承
  - 遗产
  # 物业纠纷
  - 物业费
  - 物业
  - 业主
  - 不交物业费
  # 刑事
  - 被抓
  - 拘留
  - 取保候审
  - 判刑
  - 罪
  - 报案
  # 合同/买卖
  - 合同
  - 违约
  - 退款
  - 不发货
  - 质量不合格
  # 行政/其他
  - 行政复议
  - 行政诉讼
  - 征收补偿
  - 处罚
  - 申诉
---
## 📜 使用授权声明（版权与修改权限）

> **本技能包的版权与修改权限，以 （《律师助手·使用授权协议》）为准。**
> - **授权人（七邻八舍 / 强哥）本人**：可自由使用、修改、维护、再分发本技能包。
> - **第三方（任何非授权人）**：仅可在**不修改**的前提下使用本技能包；**禁止修改、禁止派生、禁止去除版权与授权声明、未经授权禁止再分发**。
> - 第三方修改版如经平台政策允许发布，须注明差异、不得冒充原版、不享授权人背书（详见  第三节）。
> - 本软件按“现状”提供，不构成正式法律意见，重大事务须咨询执业律师。

## 🔴 第零条·强制：每次调用必先渲染「律师助手全景图」（先于一切业务分析）

> **【焊死·最高优先级】** 用户**只要说出任何一句话**（首次或之后每次），本回合的**第一条动作**必须是：在界面右侧常驻渲染「律师助手全景图」（交互式能力导航目录，12 大阶段 · 106 个关键节点（107 个技能文件 − 1 个全景图导航 = 106 个业务节点） 节点）。它优先级高于写文书、做分析、答问题——**图文并行：先出图，同时开始分析，不阻塞业务**。
> 渲染动作由脚本自动执行；发布失败时立即切换纯文本节点树兜底，**不得要求用户手动复制文件**。

**为什么模型容易漏（自检用）：** 这条规则若只写在长文档中段、渲染动作又藏在另一个文件里，模型几乎必然略过。所以本块把「怎么渲染」直接写死在下面，无需再去读  也能执行。

### 一键发布法（每回合必做）

> 用户**永远不需要手动复制任何文件**。渲染 → 发布 → 展示全自动：脚本从自身位置自动定位技能包、渲染最新源图并原子写入当前工作区（ 之外），失败时自动打印纯文本节点树兜底。

第 1 步 · 一键发布（自动渲染 + 发布到当前工作区，无需记路径）：
```bash
# 首选（自动定位、自动渲染、自动发布，运行后输出可渲染的绝对路径）：
python "<技能包目录>/scripts/present_panorama.py"
# 发布到指定目录（如 ./outputs/）：
# python "<技能包目录>/scripts/present_panorama.py" --to ./outputs
# 自检发布件（缺失/过期/位置异常自动重发）：
# python "<技能包目录>/scripts/present_panorama.py" --check-present
```

第 2 步 · 渲染发布件（路径取第 1 步输出的文件，务必在  之外）：
```
present_files → ./律师助手节点树状图.html
```

> 🚫 **禁止删除发布件（血的教训）：** 第 1 步生成的  是右侧预览的**长期依赖文件**，本回合及后续回合都靠它渲染。
> - **绝对禁止**用  /  / 清理脚本删它；作者曾因「清理测试副本」把它误删，导致预览引用失效、显示破损乱码，被用户发现「图又没了」。
> - 若需重新生成，重跑第 1 步覆盖即可（原子替换），**不要先删后建**。
> - 桌面再留一份  永久本地副本（用户可随时双击浏览器打开），作为预览重置时的兜底。

**脚本不可用时的最后手段（仅当  无法运行时才用）**：
```bash
# 手动 cp（<技能包目录> 即本 SKILL.md 所在目录）：
cp "<技能包目录>/assets/律师助手节点树状图.html" "./律师助手节点树状图.html"
# Windows(PowerShell) 兜底：
# cp (Get-ChildItem -Path $env:USERPROFILE -Recurse -Filter 律师助手节点树状图.html -ErrorAction SilentlyContinue | Select-Object -First 1).FullName "./律师助手节点树状图.html"
# macOS/Linux(bash) 兜底：
# cp "$(find ~/.workbuddy -name 律师助手节点树状图.html 2>/dev/null | head -1)" "./律师助手节点树状图.html"
```

**纯文本降级（无 HTML 预览环境 / 发布失败时，零文件操作）**：
直接输出  内的「🌳 全流程节点图」折叠块给用户，**不要求用户手动复制任何文件**。

### 📛 术语铁律（防张冠李戴）
- **「节点图 / 全景图 / 能力图 / 技能全景图」在本技能里专指**上面这份  交互式导航目录。
- **针对具体案子画的分析图，只能叫「案件分析图」**，严禁叫「节点图 / 律师助手节点图 / 全景图」——否则会和本技能的常驻导航图混淆（作者曾因此张冠李戴，给用户错发了案子图）。
- 用户说「调出节点图 / 全景图 / 目录 / 功能图」→ 永远渲染本块定义的导航目录，不是案子图。

### 🔄 全景图统计/时间轴必须随包实时更新（版本号 / 技能文件数 / 总文件数 / 时间轴「今天」）

> **【焊死·随包同步】** 全景图里展示的 **「版本号」「技能文件数」「总文件数」以及案件时间轴的「今天」参考线**，**必须跟随技能包的真实状态自动更新，尤其是版本号与时间轴日期**——每发一版（version 字段 +1）或每过一天，图里就必须同步，绝不允许写死 stale 数字（曾出现时间轴「今天」卡在昨天 07-27）。

- 这几项**过去是写死在  的 JS 常量里**（曾出现图显示 、时间轴「今天」停在 ），现已改为**由  在每次渲染时实时计算并注入**：
  - 版本号 ← 读  frontmatter 的  字段（+值）；
  - 技能文件数 ←  的实际文件数；
  - 总文件数 ← 遍历整个包、按打包排除规则（ 目录 +  文件）统计的总文件数；
  - 时间轴「今天」← 取**系统真实当日日期**（，与里程碑格式一致），注入 ，时间轴「📍 今天」参考线随之对齐。
- **模型/提交者不得手动改这三个常量**，改了也会被脚本覆盖；要更新数字，唯一的正确动作是跑脚本。
- 打包提交前，必须执行  把实时值**写回源 HTML 基线**，保证  里随包同步（见下方「提交前必做」）。

> **【固定基线·不可改】** 全景图的**版式结构与配色已固定为基线**：对话框深青蓝渐变 、边框 、阴影 ，图例/时间轴/今日标签统一青蓝冷族 。该配色**永久锁定，不再调整**；脚本每次调用只同步「版本号 / 技能文件数 / 总文件数 / 时间轴今天」四项统计字段。
> **【每次调用必做】** 用户**每一次**调用律师助手，必须先跑 （实时渲染并发布随包最新版本的图到工作区），再用「一键发布法」把该图**常驻右侧**——不可直接渲染  源文件（403）、不可跳过、不可用旧副本顶替。版本号一旦在  +1，下一轮调用发布出的图必然同步新版本号。

### ✅ 收尾自检（每回合结束前必做）
结束本轮回复前，必须确认本回合已执行过 ；**若未渲染，立即补做，不得遗漏、不得用文字描述代替。**
**发布件自检**：跑 ，发布件缺失/过期/位于隐藏目录时脚本自动重发；仍失败则直接输出纯文本节点树兜底。
**版本号一致性校验**：跑 ，确认全景图的版本号 ==  的 ，不一致立即  修复。

### 📦 提交前必做（同步数字到 legal-skills.zip）
- 每次发版/重建提交稿前，**先**跑 ，把实时「版本号 / 技能文件数 / 总文件数」写回  基线；
- 再用打包脚本全量重建 （以源目录为准  全量写入，排除不安全路径）；
- 解包核对：全景图  与  版本号、 数、包总文件数三者一致，版本号四处（SKILL.md / manifest / _plugin_base / 全景图）一致。

> 设计意图与无 HTML 预览时的纯文本降级见 ；开场动作编排见 「三段式引导入口 · 开场动作」。

## ⚠️ 强制规则：节点图右侧常驻（任何一次调用都触发）

> **（执行细则见上方「🔴 第零条 · 强制」）** 不论用户是**首次**还是**之后每一次**调用律师助手，只要用户说出**任何一句话**，节点图必须**同时**出现在界面右侧栏并**常驻**；用户可点右上角「收起」隐藏，或点右下角「📑 目录」浮标随时唤回。
> - 这是技能包的**顶层强制行为**，优先级高于一切分支判断与场景识别；任何入口、任何案由、任何模式都不得省略。
> - 渲染动作由  定义，调用链路由  在每次激活时同步执行（详见两文件「展示与唤回规则 / 开场动作」）。
> - 若环境不支持 HTML 预览，则输出  内的纯文本节点树兜底，但**常驻右侧**这一要求不变。

## 快速选择

| 你的需求 | 推荐Agent | 核心产出 | 预计耗时 | 工作量 |
|---------|----------|---------|---------|-------|
| 借钱不还 | 41-民间借贷纠纷 | 起诉状+证据清单+风险提示 | 5-10分钟 | ● |
| 被车撞了 | 05-交通事故责任纠纷 | 赔偿清单+起诉状 | 5-10分钟 | ● |
| 公司辞退 | 15-劳动争议仲裁程序管理 | 仲裁申请书+计算表 | 5-10分钟 | ● |
| 离婚分割财产 | 54-离婚纠纷 | 起诉状+财产分割清单 | ~15分钟 | ●● |
| 家人被拘留 | 09-侦查阶段辩护 | 取保候审申请书+行动指南 | 5-10分钟 | ●●● |
| 合同纠纷 | 03-买卖合同纠纷 | 起诉状+违约责任分析 | 10-15分钟 | ●● |
| 物业费纠纷 | 43-物业服务合同纠纷 | 答辩状+抗辩策略 | 5-10分钟 | ● |
| 系统分析案件 | 70-案件深度分析系统 | 九步法攻防全景图 | ~90分钟 | ●●● |
| 开庭要实时记要点 | 109-庭审实时辅助系统 | 实时转写+要点浮窗+庭审速记 | 庭审全程 | ●●● |
| 刑事案子系统分析 | 110-刑事案件深度分析系统 | 犯罪构成拆解+辩护策略+量刑分析 | ~90分钟 | ●●● |

## 真实用户案例

> **张三**——民间借贷纠纷：输入“借给朋友5万有借条不还”，5分钟后拿到起诉状初稿+证据清单。说“导出Word文档”转为正式文书。
>
> **李四**——交通事故：输入“被车撞了对方全责保险公司不赔误工费”，10分钟后拿到赔偿清单+起诉状。AI自动计算了误工费、护理费、交通费等各项赔偿。
>
> **王五**——劳动争议：输入“公司辞退只给1个月补偿干了3年”，15分钟后拿到仲裁申请书+经济补偿计算表。AI提醒了仲裁时效1年、未休年假折现等3个他不知道的权益。


## 跨会话继续

每次对话结束时AI会生成案件状态卡。复制保存后，下次对话时先粘贴状态卡，AI会自动恢复进度，无需重新描述案情。

## 快速上手

> 👨**律师助手：** 我是你的AI法律助理，由107个技能文件组成。
> 不知道怎么开始？**直接说你的问题就行**，不用选技能、不用法律术语。

`
“他欠我5万不还，有借条，怎么办”   → 自动匹配民间借贷专家
“我被公司辞退了没给赔偿”           → 自动匹配劳动争议专家
“我儿子被警察带走了”                → 自动匹配刑事法律专家
`

**三步就走完：**
1. 📝 **说情况** — 用你自己的话描述问题
2. ❓ **答问题** — AI问你几个关键问题（一次只问一个）
3. 📄 **拿结果** — 法律分析 + 文书初稿

> 🧭 **标准办案链路（律师视角，已焊死在总控路由）**：① 你说案情 → AI 生成《案情确认书》→ ② 你提交证据 → **自动触发「87-案卷精读防漏」做证据清单 + 要件矩阵（防漏细节）** → ③ 进入分析 → **自动触发「70-案件深度分析」走要件九步法** → ④ 凡引法条/案例 → **必过「96-法条交叉核验」多源比对、案例强制权威源**（防幻觉）→ ⑤ 文书初稿 + 质量核验 + 交付。每一步都是下一步的闸门，不跳阶、不漏材料。

> 💡 **不想打字？** 打开语音输入，直接说“借钱不还怎么办”就行。
> 💡 **不确定选什么？** 总控路由自动匹配最合适的专家。

## 依赖

```bash
pip install openai python-docx
```

> 部分功能（如法规库检索）需额外配置 MCP 工具（华宇元典/北大法宝），详见 。

## MCP 配置（pkulaw / yuandian）

本技能包的法规与案例实时核验依赖两个 MCP 数据源，均为**可选**——未配置时自动降级为离线法规库（详见  的「离线降级策略」）。

| MCP 连接器 | 作用 | 工具命名空间 |
|:-----------|:-----|:-------------|
| 北大法宝·法律智能检索（） | 法规原文 + 案例检索 |  或  |
| 华宇元典法律数据（） | 向量检索 + 案例语义检索 |  或  |

**配置方式（二选一）：**
1. **WorkBuddy 客户端（推荐）**：左侧「连接器」面板搜索「北大法宝·法律智能检索」「华宇元典法律数据」，点击连接并在弹窗中「信任」即可，无需手动改配置。
2. **手动配置**：在  的  中加入对应条目（server 名分别为 、），具体字段以官方文档为准。

> 工具命名空间随部署形态自适应（详见 「方案D：WorkBuddy 单一命名空间」）。技能运行时会自动探测  与  两种前缀，无需手动切换。
> 任一源不可用时，技能按「双源核验 → 离线降级」链路透明降级并标注置信度，绝不静默假装已核验。

