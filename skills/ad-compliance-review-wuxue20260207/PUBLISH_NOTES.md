# 广告合规审查宝 · 发布到 SkillHub 市场 说明

> 本文件仅作发布辅助，**不随技能运行逻辑生效**；发布前可保留、发布后可删除。

## 当前状态（已就绪）
- 版本号三处一致：**1.3.0**（`SKILL.md` frontmatter / `_meta.json` / `_skillhub_meta.json`）
- 发布物齐备：`SKILL.md`、`CHANGELOG.md`、`config.example.json`、3 个脚本、`references/`、`sample_data/`
- 安全：无真实密钥（仅 `YOUR_API_KEY_HERE` 占位），无 `config.json` 误提交
- 已通过端到端验证（审查 / 报告 / 在线页 / 路径校验 / 重试）

## 为什么由你在客户端发布（而非 agent 代发）
- agent 可调用的市场工具仅支持 **search / install**，无 publish 动作；
- `SkillManage` 仅做本地 list/create/modify/delete，不推送市场；
- 本机无发布用 CLI；WorkBuddy 为 Electron 客户端，发布需绑定你账号的会话鉴权。
- 因此发布请在 **WorkBuddy 客户端**内完成（见下方步骤）。

## 客户端发布步骤（参考）
1. 打开 WorkBuddy 客户端，确保已登录你的账号。
2. 进入「技能 / Skills」管理界面，找到 **广告合规审查宝（slug: wuxue20260207）**。
3. 对该技能选择「发布到市场 / 更新到市场」（若已发布过则走「更新」，版本将取本地 1.3.0）。
4. 在市场提交表单中粘贴下方「市场文案」，确认分类与标签，提交。
5. 若平台有审核，等待审核通过后即上线 / 更新生效。

## 市场文案（可直接粘贴）

**名称**：广告合规审查宝（Ad Compliance Review）
**一句话简介**：广告/营销公司上线前「AI 合规闸」——对照《广告法》与品牌红线，自动排查违禁词、绝对化用语、医疗功效暗示、平台限制，输出修改建议；支持单条审查、批量体检报告、零依赖在线自助页。

**标签 / 分类**：广告合规 · 广告法 · 文案审查 · 品牌调性 · 获客钩子 · 零依赖 · Python

**版本**：1.3.0

**更新亮点（v1.3.0）**：
- 🔁 LLM 改写自动重试：网络抖动 / 限流(429) / 5xx 等瞬时故障按指数退避自动重试（默认 3 次），不再因一次网络抖动直接失败
- 🛡️ `--client` 路径显式校验：路径不存在 / 指向文件 → 明确报错并退出（exit 2）；目录缺词表 → 明确【警告】后回退内置基线，不再静默失败
- 🌐 在线自助页支持 `--client` 专属词库 + 启动校验，行为对齐 CLI
- 📦 新增 `config.example.json` 配置模板与 `CHANGELOG.md` 版本说明

**功能亮点（长期）**：
- 单条文案审查（关键词 + 正则，零依赖、纯标准库）
- 批量生成可发客户的 HTML 合规体检报告（含饼图/柱状图/明细，可打印 PDF）
- 零依赖在线自助提交页（客户自助上传文案生成报告，获客钩子）
- 可选接入大模型产出合规改写文案
- 内置绝对化 / 医疗功效通用基线 + 客户专属词库合并

## 发布前检查清单
- [ ] 版本号三处 = 1.3.0
- [ ] `CHANGELOG.md` 已更新到 1.3.0
- [ ] 无真实 API Key / 密钥入库（仅 `config.example.json` 占位）
- [ ] 脚本端到端验证通过（已做）
- [ ] 市场文案已按上方填好
