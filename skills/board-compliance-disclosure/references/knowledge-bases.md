# 本 Skill 绑定的知识库

| 订阅知识库 | 检索ID（内部） | 主要内容 |
|---|---|---|
| 深证信（巨潮）公告库 | `7416670364645874` | 上市公司公告、问询函及回复、监管措施等公开披露资料 |

检索须严格限定在该库。

## ⚠️ 准确性与可移植性（安装/使用前必读）

1. **本 ID 是作者账号下的实例 ID，不是通用常量。** 知识库 ID 与订阅者账号绑定：他人在自有 IMA 订阅「搬砖小能手」后，其账号下该知识库的 ID 通常不同于上表的 `7416670364645874`。安装本 Skill 后，须用 `mcp__ima-mcp__get_knowledge_base_list`（type=KBT_SUBSCRIBED_JOIN_KB）查出自己账号下「深证信（巨潮）公告库」的真实 ID，并替换本文件中的 ID，否则检索会命中他人库或返回空/权限错误。
2. **先用预检验证可用性。** 加载 Skill 后按 `references/IMA检索执行规范.md` 发起一次轻量 `search_knowledge`（cursor=""）确认 ID 可用；返回 0 条/权限错误即按降级流程处理（见 SKILL「KB 预检与降级」）。
3. **报告正文严禁出现数字 ID**，仅保留知识库名称「深证信（巨潮）公告库」。
